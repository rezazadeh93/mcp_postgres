[project]
name = "hermes-db"
version = "0.1.0"
description = "Lightweight MCP server for Hermes program research over local Postgres"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=2.0,<4",
    "psycopg[binary]>=3.2",
]

[tool.uv]
package = false
